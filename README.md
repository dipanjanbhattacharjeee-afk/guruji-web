# guruji-web
A free, serverless private tutor management system. There is no backend server, no paid database, no hosting cost. Everything — all student data, class files, fee records — is stored in a single JSON file on the **Tutor's own Google Drive**. The app runs entirely in the browser.
